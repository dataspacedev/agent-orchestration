---
phase: 01-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-controller/api/v1alpha1/agentdirectory_types.go
  - agent-controller/api/v1alpha1/clusterpolicy_types.go
  - agent-controller/api/v1alpha1/agentpolicy_types.go
  - agent-controller/api/v1alpha1/groupversion_info.go
  - agent-controller/zz_generated.deepcopy.go
  - config/crd/bases/agents.orchestration.io_agentdirectories.yaml
  - config/crd/bases/agents.orchestration.io_clusteragentpolicies.yaml
  - config/crd/bases/agents.orchestration.io_agentpolicies.yaml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-11

must_haves:
  truths:
    - "AgentDirectory CRD can be applied to a cluster with fields: agentName, version, url, skills[], cardHash, readyAt"
    - "ClusterAgentPolicy CRD is cluster-scoped with full P1 schema: spec.auth (tokenAudience, mode), spec.otel (endpoint, sampling), spec.resilience (timeoutMs, retries)"
    - "AgentPolicy CRD is namespace-scoped with identical spec fields to ClusterAgentPolicy"
    - "make generate runs without error and produces matching zz_generated.deepcopy.go"
    - "CI blocks any PR that adds *_types.go without running make generate (git diff --exit-code)"
  artifacts:
    - path: "agent-controller/api/v1alpha1/agentdirectory_types.go"
      provides: "AgentDirectory and AgentDirectoryList types with kubebuilder markers"
      exports: ["AgentDirectory", "AgentDirectoryList", "AgentDirectorySpec"]
    - path: "agent-controller/api/v1alpha1/clusterpolicy_types.go"
      provides: "ClusterAgentPolicy and ClusterAgentPolicyList types, scope=Cluster"
      exports: ["ClusterAgentPolicy", "ClusterAgentPolicyList", "ClusterAgentPolicySpec", "AuthSpec", "OTELSpec", "ResilienceSpec"]
    - path: "agent-controller/api/v1alpha1/agentpolicy_types.go"
      provides: "AgentPolicy and AgentPolicyList types, scope=Namespaced"
      exports: ["AgentPolicy", "AgentPolicyList", "AgentPolicySpec"]
    - path: "config/crd/bases/agents.orchestration.io_clusteragentpolicies.yaml"
      provides: "CRD manifest for ClusterAgentPolicy"
      contains: "scope: Cluster"
  key_links:
    - from: "agent-controller/api/v1alpha1/groupversion_info.go"
      to: "agentdirectory_types.go, clusterpolicy_types.go, agentpolicy_types.go"
      via: "SchemeBuilder.Register() calls"
      pattern: "SchemeBuilder\\.Register"
    - from: "agent-controller/api/v1alpha1/zz_generated.deepcopy.go"
      to: "all three new type files"
      via: "make generate (controller-gen)"
      pattern: "func.*DeepCopyInto"
---

<objective>
Define the three new CRD Go types (AgentDirectory, ClusterAgentPolicy, AgentPolicy) that all subsequent Wave 2 work depends on. Run make generate to produce deepcopy methods and CRD manifests. Add CI enforcement.

Purpose: All Go work in Wave 2 (AgentReconciler, registry-announcer, AgentPolicyReconciler) imports these types. Without this plan complete and generate run, nothing compiles.
Output: Three new type files registered in the scheme, generated deepcopy file updated, CRD YAML manifests in config/crd/bases/, CI workflow added.
</objective>

<execution_context>
@/Users/justinbrewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinbrewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-runtime-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing CRD registration pattern from agent-controller/api/v1alpha1/groupversion_info.go -->
<!-- Copy SchemeBuilder.Register() call for each new type -->

From agent-controller/api/v1alpha1/agent_types.go (existing kubebuilder marker pattern):
```go
// CRD group: agents.orchestration.io/v1alpha1 — all new types must use this group

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:resource:scope=Namespaced,shortName=ad

type AgentDirectory struct {
    metav1.TypeMeta   `json:",inline"`
    metav1.ObjectMeta `json:"metadata,omitempty"`
    Spec   AgentDirectorySpec   `json:"spec,omitempty"`
    Status AgentDirectoryStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true
type AgentDirectoryList struct {
    metav1.TypeMeta `json:",inline"`
    metav1.ListMeta `json:"metadata,omitempty"`
    Items []AgentDirectory `json:"items"`
}

func init() {
    SchemeBuilder.Register(&AgentDirectory{}, &AgentDirectoryList{})
}
```

From agent-controller/api/v1alpha1/agent_types.go:
```go
// ptr[T any](v T) *T helper is at agent_controller.go:452
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AgentDirectory and ClusterAgentPolicy/AgentPolicy CRD type files</name>
  <files>
    agent-controller/api/v1alpha1/agentdirectory_types.go,
    agent-controller/api/v1alpha1/clusterpolicy_types.go,
    agent-controller/api/v1alpha1/agentpolicy_types.go,
    agent-controller/api/v1alpha1/groupversion_info.go
  </files>
  <behavior>
    - agentdirectory_types.go: AgentDirectorySpec has fields agentName string, version string, url string, skills []string, cardHash string, readyAt *metav1.Time; kubebuilder markers: scope=Namespaced, shortName=ad; printcolumns: CardHash (string, .spec.cardHash), ReadyAt (date, .spec.readyAt), Age (date, .metadata.creationTimestamp)
    - clusterpolicy_types.go: ClusterAgentPolicySpec has auth AuthSpec, otel OTELSpec, resilience ResilienceSpec; AuthSpec has tokenAudience string, mode string; OTELSpec has endpoint string, sampling float64; ResilienceSpec has timeoutMs int32, retries int32; kubebuilder markers: scope=Cluster, shortName=cap
    - agentpolicy_types.go: AgentPolicySpec is identical struct to ClusterAgentPolicySpec; kubebuilder markers: scope=Namespaced, shortName=ap; AgentPolicy is the namespace-scoped counterpart
    - groupversion_info.go: all three new types added to SchemeBuilder.Register() alongside existing Agent type
    - All three files follow exact same file structure as agent_types.go (TypeMeta, ObjectMeta, Spec, Status, List type, init() registration)
    - All types use group agents.orchestration.io/v1alpha1 (inherited from groupversion_info.go GroupVersion var)
  </behavior>
  <action>
    Create the three type files in agent-controller/api/v1alpha1/:

    agentdirectory_types.go:
    - Package declaration: package v1alpha1
    - Imports: metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
    - Kubebuilder markers: +kubebuilder:object:root=true, +kubebuilder:subresource:status, +kubebuilder:resource:scope=Namespaced,shortName=ad
    - Printcolumn markers for CardHash, ReadyAt, Age
    - AgentDirectorySpec struct with json tags (agentName, version, url, skills omitempty, cardHash omitempty, readyAt omitempty pointer)
    - AgentDirectoryStatus struct (empty, reserved for future conditions)
    - AgentDirectory and AgentDirectoryList types following agent_types.go pattern
    - init() calling SchemeBuilder.Register(&AgentDirectory{}, &AgentDirectoryList{})

    clusterpolicy_types.go:
    - Package declaration: package v1alpha1
    - AuthSpec struct: TokenAudience string `json:"tokenAudience,omitempty"`, Mode string `json:"mode,omitempty"`
    - OTELSpec struct: Endpoint string `json:"endpoint,omitempty"`, Sampling float64 `json:"sampling,omitempty"`
    - ResilienceSpec struct: TimeoutMs int32 `json:"timeoutMs,omitempty"`, Retries int32 `json:"retries,omitempty"`
    - ClusterAgentPolicySpec struct: Auth AuthSpec, OTEL OTELSpec, Resilience ResilienceSpec (all omitempty)
    - Markers: +kubebuilder:resource:scope=Cluster,shortName=cap
    - ClusterAgentPolicyStatus struct (empty)
    - ClusterAgentPolicy, ClusterAgentPolicyList, init() registration

    agentpolicy_types.go:
    - AgentPolicySpec is SAME fields as ClusterAgentPolicySpec (copy, do not embed)
    - Markers: scope=Namespaced, shortName=ap
    - AgentPolicy, AgentPolicyList, init() registration

    groupversion_info.go: Add Register calls for all three new pairs alongside existing Agent/AgentList:
    - SchemeBuilder.Register(&AgentDirectory{}, &AgentDirectoryList{})
    - SchemeBuilder.Register(&ClusterAgentPolicy{}, &ClusterAgentPolicyList{})
    - SchemeBuilder.Register(&AgentPolicy{}, &AgentPolicyList{})

    After creating files, run: cd agent-controller && make generate && make manifests

    This produces zz_generated.deepcopy.go with DeepCopyInto/DeepCopyObject for all new types, and config/crd/bases/ YAML files. Verify config/crd/bases/agents.orchestration.io_clusteragentpolicies.yaml contains `scope: Cluster`.
  </action>
  <verify>
    <automated>cd /Users/justinbrewer/Documents/repos/agent-orchestration/agent-controller && make generate && git diff --exit-code -- zz_generated.deepcopy.go api/v1alpha1/ 2>/dev/null; go build ./...</automated>
  </verify>
  <done>
    Three type files exist. make generate runs without error. zz_generated.deepcopy.go contains DeepCopyInto for AgentDirectory, ClusterAgentPolicy, AgentPolicy. CRD YAML in config/crd/bases/ has scope: Cluster for ClusterAgentPolicy. go build ./... passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CI workflow enforcing make generate on *_types.go changes</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    Create .github/workflows/ci.yml (creating the .github/workflows/ directory as needed).

    The workflow must:
    1. Trigger on: pull_request targeting main (also push to main for badge purposes)
    2. Have a job named "generate-check" with steps:
       - actions/checkout@v4
       - actions/setup-go@v5 with go-version from agent-controller/go.mod
       - Run: cd agent-controller && make generate && make manifests
       - Run: git diff --exit-code (fails if generated files diverged)
       - On failure, emit message: "Run 'make generate && make manifests' in agent-controller/ and commit the result"
    3. Have a job named "test" that runs: cd agent-controller && make test
    4. Both jobs run in parallel (no needs dependency between them)

    The intent from ROADMAP.md: "CI check: make generate && git diff --exit-code blocking any PR that adds *_types.go without re-running controller-gen" — make this unconditional (runs on every PR, not just when types changed), which is simpler and more reliable.
  </action>
  <verify>
    <automated>cat /Users/justinbrewer/Documents/repos/agent-orchestration/.github/workflows/ci.yml | grep -c "git diff --exit-code"</automated>
  </verify>
  <done>
    .github/workflows/ci.yml exists with a job that runs make generate && git diff --exit-code. File is valid YAML (no syntax errors). The generate-check job would catch any undiscovered *_types.go additions that skip regeneration.
  </done>
</task>

</tasks>

<verification>
Run full check after plan completes:
```bash
cd /Users/justinbrewer/Documents/repos/agent-orchestration/agent-controller && go build ./... && make test
grep -l "scope: Cluster" config/crd/bases/*.yaml
cat config/crd/bases/agents.orchestration.io_agentdirectories.yaml | grep -A3 "cardHash"
```
</verification>

<success_criteria>
- Three new type files exist in agent-controller/api/v1alpha1/ with correct kubebuilder markers
- make generate runs clean; zz_generated.deepcopy.go updated
- config/crd/bases/ has three new CRD YAML files; ClusterAgentPolicy has scope: Cluster
- go build ./... passes in agent-controller
- .github/workflows/ci.yml enforces git diff --exit-code after make generate
</success_criteria>

<output>
After completion, create .planning/phases/01-runtime-foundation/01-01-SUMMARY.md
</output>
